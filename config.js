module.exports = {
    devlopment: {
        PORT: 3000,
        HOST: "localhost",
        NODE_ENV: "devlopment"
    },
    production: {
        PORT: 8080,
        HOST: "localhost",
        NODE_ENV: "production"
    }
}